// Verifique se os dois valores passados por parametro sao maior ou igual a 6.
// Se sim, retorne a mensagem: true.
// Se nao, retorne a mensagem: false.

function sameValues(val1, val2) {
  // escreva seu codigo aqui
}

// Escreva um programa que receba um valor em metros e o retorne convertido em centimetros.!

function convertToCm(meters) {
  // escreva seu codigo aqui
}

// Escreva um programa que receba um numero e retorne seu dobro, triplo e raiz quadrada.
// O retorno deve ser no formato abaixo especificado.
// Exemplo: 2 => "O dobro de 2 é 4, o triplo é 6 e a raiz quadrada é 1.4142135623730951"

function dobroTriploRaiz(numero) {
  // escreva seu codigo aqui
}

// Crie uma funcao que receba um nome completo e retorne um email no seguinte formato:
// nome_sobrenome@aulao.com
// Todas as letras devem ser minusculas!

function createEmail(fullName) {
  // escreva seu codigo aqui
}

// Na copa mundial de Quadribol, cada vitoria vale 7 pontos, cada empate 3 pontos e derrota 0 pontos
// eslint-disable-next-line max-len
// Escreva uma funcao que receba o numero de vitorias, empates e derrotas e retorne a pontuacao final.

function getPoints(wins, ties, losses) {
  // escreva seu codigo aqui
}

// eslint-disable-next-line max-len
// Crie uma funcao que receba um array de numeros e retorne a soma do maior numero com o menor numero.

function sumMinMax(numbers) {
  // escreva seu codigo aqui
}

// Implemente a funcao polarEncode que codifica frase para o codigo Zenit Polar.
// O retorno final deve ser em letras minusculas.

// Quando a letra for Z, troque por P.
// Quando a letra for E, troque por O.
// Quando a letra for N, troque por L.
// Quando a letra for I, troque por A.
// Quando a letra for T, troque por R.

// Quando a letra for P, troque por Z.
// Quando a letra for O, troque por E.
// Quando a letra for L, troque por N.
// Quando a letra for A, troque por I.
// Quando a letra for R, troque por T.

function polarEncoder(sentence) {
  // escreva seu codigo aqui
}

// Implemente a funcao encodeLetter que codifica as vogais para o o codigo Aulao.
// Implemente a funcao decodeLetter que decodifica o codigo Aulao para as vogais.

// Quando a letra for A, troque por @.
// Quando a letra for E, troque por 3.
// Quando a letra for I, troque por !.
// Quando a letra for O, troque por 0.
// Quando a letra for U, troque por Ʉ.

function encodeLetter(letter) {
  // escreva seu codigo aqui
}

function decodeLetter(letter) {
  // escreva seu codigo aqui
}

// Some todos os valores de um array de numeros e retorne o valor multiplicado por 2.

function sumAndMultiplyBy2(numbers) {
  // escreva seu codigo aqui
}

// Crie uma funcao que receba um array de numeros e retorne a soma dos numeros pares.

function sumEvenNumbers(numbers) {
  // escreva seu codigo aqui
}

// Crie uma funcao que receba um array de numeros e retorne a soma dos numeros impares.

function sumOddNumbers(numbers) {
  // escreva seu codigo aqui
}

module.exports = {
  sameValues,
  convertToCm,
  dobroTriploRaiz,
  createEmail,
  getPoints,
  sumMinMax,
  polarEncoder,
  encodeLetter,
  decodeLetter,
  sumAndMultiplyBy2,
  sumEvenNumbers,
  sumOddNumbers,
};
